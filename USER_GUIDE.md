# Keep Fit 使用指南

欢迎使用 Keep Fit 健身打卡应用。

## 访问

正式访问地址以 Vercel 项目绑定的域名为准。建议在手机浏览器打开后使用“添加到主屏幕”安装 PWA。

## 数据说明

- 本地浏览器保存当天训练草稿、UI 设置、AI 对话缓存和离线容灾数据。
- 完成训练后，训练记录会写入 Notion。
- AI 教练只接收当次聊天或训练 review 中显式传入的上下文。
- 如果开启训练提醒，Web Push 订阅会保存到服务端。

## 日常使用

1. 打开 Keep Fit，确认今日 A/B/C 或 Recovery 计划。
2. 点击「开始训练」进入训练模式。
3. 填写每组重量和次数，完成后可提交训练反馈。
4. 完成 Notion 同步后，可在教练页查看 AI review。

## 常见问题

**训练视频不能自动播放？**  
点击视频区域开始播放。iOS 可能需要在用户点击后播放。

**更换设备后数据能同步吗？**  
Notion 中的正式训练记录可以通过登录账号访问；本地草稿不会跨设备同步。

**清空浏览器数据会怎样？**  
当天未同步草稿和本地 AI 对话缓存可能丢失；已写入 Notion 的训练记录不会因此丢失。

祝你健身愉快！💪
